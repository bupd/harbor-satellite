apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
spec:
  containers:
    - name: nginx
      image: localhost:8585/library/nginx:latest
      ports:
        - containerPort: 80
